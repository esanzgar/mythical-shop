import { CurrencyRatePipe } from './currency-rate.pipe';

describe('CurrencyRatePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyRatePipe();
    expect(pipe).toBeTruthy();
  });
});
